# Neon / Supabase / local Postgres
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/fox_kisem?schema=public"

# Min 16 chars; use a long random string in production
SESSION_SECRET="replace-with-a-long-random-secret-key"

# Set true only for controlled onboarding; first user can always register when DB has zero users
ALLOW_REGISTER="false"

# Email Service - Resend (Primary)
RESEND_API_KEY="re_..."
RESEND_FROM_EMAIL="A-CMP <iea@iitgn.ac.in>"

# Email Service - SMTP Nodemailer Fallback (Optional)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_USER="iea@iitgn.ac.in"
SMTP_PASS="xxxx xxxx xxxx xxxx"

NEXT_PUBLIC_API_URL=http://localhost:3000
